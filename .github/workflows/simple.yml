name: Simple Command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
      run: echo "welcome"
      - name: multiline
      run: |
        node -v
        npm -v
